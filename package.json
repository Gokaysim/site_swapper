{
  "name": "siteswapper_new",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "getInvoke": "serverless invoke local --function hello -p ./sampleRequests/blog.json",
    "postInvoke": "serverless invoke local --function hello -p ./sampleRequests/commentPost.json",
    "searchInvoke": "serverless invoke local --function hello -p ./sampleRequests/search.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.3.0"
  }
}
